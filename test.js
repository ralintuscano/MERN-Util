// path = path.join(CURRENT_WORKING_DIR, "/dist");
// import path from "path";
const path = require("path");
const rel = path.join(process.cwd(), "/dist");
console.log(rel);

// console.log(process.cwd());
